# Afridock — Progress Tracker

> **Every AI agent MUST read this file before writing any code**, and MUST update it after completing meaningful work (feature, epic story, migration, infra change). This is the single source of truth for what exists, what is in flight, and what comes next. The execution plan ([Afridock_Implementation_Execution_Plan.md](Afridock_Implementation_Execution_Plan.md)) defines *what to build*; this file records *what has been built*.
>
> **This is enforced mechanically, not just by convention** — see "Enforcement" below.

## Enforcement

Three deterministic checks back this policy; none rely on an agent choosing to comply:

1. **Claude Code hook** (`.claude/settings.json` + `.claude/hooks/`): a `PreToolUse` hook blocks any `Write`/`Edit` under `apps/`, `packages/`, or `infra/` in a session until a `PostToolUse` hook has recorded that `progress.md` was read in that same session. Enforces "read before writing any code" for AI agents using Claude Code specifically.
2. **Git pre-commit hook** (`.githooks/pre-commit`, activated via `git config core.hooksPath .githooks` — run once per clone, e.g. `make hooks-install`): blocks any commit that stages changes under `apps/`, `packages/`, or `infra/` unless `progress.md` is staged in the same commit. Enforces "update after completing work" for any committer (human or agent), regardless of tool. Escape hatch: `git commit --no-verify` for changes that genuinely don't warrant a progress.md update.
3. **CI check** (`.github/workflows/ci.yml`, job `progress-tracker`): re-runs the same check against the PR diff. Catches the case where hooks aren't installed locally or `--no-verify` was used — nothing merges without it passing.

## Current status

**Phase: 0 (complete) → Phase 1 — Core MVP (E1–E4 complete) → Phase 2 — Governance (foundational slice: low-cost-AI constraint, E5, E6 complete)**

Last updated: 2026-07-24 (Phase 2 foundational slice: low-cost-open-source-AI constraint, E5 RBAC/API Keys, E6 Audit/Cost; + self-hosted NVIDIA NIM (DGX Spark, now genuinely live) + NVIDIA-hosted-cloud inference profiles; + local-dev signup auto-verify fix; + a real stream()-fallback bug and a stale-frontend-default bug found and fixed)

## Completed

### Phase 0 — Validation & Foundations
- [x] Monorepo scaffolding: `apps/api` (FastAPI), `apps/web` (React + Vite), `packages/shared`, `infra/terraform`
- [x] Docker Compose stack (Postgres + pgvector, Redis, API, web) via `make dev`
- [x] FastAPI skeleton: `/healthz` endpoint, structured logging, config, async SQLAlchemy session setup, Alembic wiring (no migrations yet)
- [x] Backend test harness: pytest + pytest-bdd (health feature/steps)
- [x] Frontend shell: React app, Tailwind, Vitest + Testing Library setup
- [x] Makefile targets: `dev`, `down`, `test`, `lint`, `fmt`, `migrate`
- [x] Terraform skeleton in `infra/terraform` (af-south-1)

### Pre-Phase-1 groundwork — patterns adapted from Dify (langgenius/dify)
Reference architecture research (4 parallel agents against a shallow clone) + implementation, feeding E2–E5. **Not** full epic completion — see caveats below each item.

- [x] **Inference abstraction** (`apps/api/src/afridock_api/inference/`): `ModelProfileRegistry` (static YAML registry, `profiles.yaml`) mapping plan model names → LiteLLM model strings; `FallbackChain` + cooldown-based failover (mirrors Dify's `LBModelManager`); `InferenceClient` wrapping `litellm.acompletion`/streaming with normalized `InferenceError` hierarchy; Fernet-based credential encryption (`credentials.py`). Unit-tested (profiles/fallback/credentials), no live provider calls made. *Wired to a real endpoint and given a stub-mode fallback in the Phase 1 pass below; still no real HF/vLLM credentials configured (works fine without them, see below).*
- [x] **Chat DB schema** (`apps/api/src/afridock_api/db/models/{conversation,message,provider}.py` + hand-written migration `alembic/versions/0001_conversations_messages_providers.py`): one row per turn (departs from Dify's query/answer-pair rows) with per-message model attribution, Postgres RLS policies on `org_id`, real `tsvector`/pgvector `Vector(1536)` columns + HNSW/GIN indexes, `provider_credentials`/`model_profile_overrides`/`inference_usage_logs` tables. Verified: tables register on `Base.metadata`, migration is the sole Alembic head, ruff/black/mypy/pytest all pass. *Now run against a live Postgres for real, with the FK constraints added, in the Phase 1 pass below.*
- [x] **Chat UI streaming + markdown** (`apps/web/src/{lib/sse.ts,hooks/useChatStream.ts,hooks/useAutoScroll.ts,components/chat/}`): `parseSSEStream` (fetch+ReadableStream line-buffered SSE parsing, ported from Dify's `handleStream`), Zustand+Immer `chatStore` (autoFreeze disabled, same reason as Dify), `MarkdownRenderer` wrapping `streamdown`/`@streamdown/code` (Dify's actual markdown stack — handles incomplete markdown mid-stream natively, no custom balancing logic needed), `MessageList`/`MessageItem` (memoized)/`ChatPanel`/`ChatInput`/`StopButton`. Build/lint/test all pass; unit tests cover SSE chunk-boundary recovery and store token-append isolation. *Wired to a real backend endpoint in the Phase 1 pass below.*
- [x] **Admin dashboard: members/RBAC** (`apps/web/src/{lib/permissions.ts,store/authStore.ts,hooks/useHasCapability.ts,services/members.ts,components/members/}`): flat `Role → Capability[]` map (simpler than Dify's granular permission-key RBAC, appropriate since the plan only needs Admin/User/Viewer), `MembersTable`/`InviteMemberDialog` (Radix Dialog/AlertDialog)/`RoleBadge`/`WorkspaceSwitcher`. Build/lint/test all pass; unit tests cover capability resolution. Routing superseded by the design-unification pass below (moved from `/settings/*` to a top-level `/team` route). *`role` now comes from a real session and `services/members.ts` hits real endpoints, both landed with E1 in the Phase 1 pass below — full E5 (API keys, granular audit) is still Phase 2 scope.*

New dependencies: backend — `pgvector`, `cryptography`, `pyyaml` (added to `pyproject.toml`); frontend — `streamdown`, `@streamdown/code`, `immer`, `@radix-ui/react-dialog`, `@radix-ui/react-alert-dialog`, `clsx`, `tailwind-merge`, `@hookform/resolvers` (added to `package.json`).

### Design unification — reconciled Dify-adapted `apps/web` with the design mockup
The user supplied a fully-worked visual prototype ("AfricaAI Platform (standalone).html", a Figma-Make-style bundle export — not real source code) with a "Classical" editorial theme and a 10-item nav that independently converged on almost the same epic breakdown as the plan. Reconciled per explicit user decisions (asked via AskUserQuestion since guessing wrong meant real rework): adopt the mockup's visual design via a *swappable token layer* rather than either discarding it or hardcoding it; build the *full nav shell now* with phase-gated placeholder pages for anything not yet in scope; treat *"Projects"* (a mockup nav item with no epic in the plan) as a nav placeholder only, its real data model deferred to Phase 4 as new epic **E20** (see the plan and the decision below); leave *out-of-phase UI* (the mockup's offline-queue banner and language picker in the chat screen — Phase 3/4 features) out of the Phase-1 chat screen entirely.

- [x] **Design-token layer** (`apps/web/src/styles/theme.css`, `tailwind.config.js`, `hooks/useTheme.ts`): CSS custom properties for the Classical theme (Cormorant Garamond/Lora fonts, cream/gold palette) as the default `:root`, with a `:root[data-theme="dark"]` override repurposing the original dark slate/emerald palette onto the same token names — proven swappable via a working sidebar toggle (`useTheme`, localStorage-backed, guards `localStorage` access since it throws in some test/private-browsing contexts), not just plumbed. Verified via Playwright: computed styles and cropped-element screenshots confirm both themes render correctly (a full-page screenshot's compression initially made dark-mode nav text look wrongly low-contrast — checked via `getComputedStyle` and an element-level screenshot before concluding it wasn't a bug).
- [x] **AppShell + Sidebar** (`apps/web/src/components/layout/{AppShell,Sidebar,PageHeader,PageContainer,ApiStatusIndicator}.tsx`): single persistent sidebar (replacing the old nested `SettingsLayout`) with all 10 mockup nav items — Assistant, Conversations, Projects, Workflows, Usage & Cost, Audit Log, Team & Roles, Integrations, Billing, Resilience — grouped into Workspace/Automation/Insights/Administration sections. A `PAGE_META` lookup derives each page's kicker/title for one shared header. The old `/healthz` check moved from the chat header into a small sidebar-footer `ApiStatusIndicator`.
- [x] **Route restructure** (`apps/web/src/routes/router.tsx`): `/chat` (Assistant, model picker + re-themed chat components), `/conversations` (new real-ish list page + `services/conversations.ts`), `/team` (Members functionality + 3 role-explainer cards + an API-Keys tab, replacing the old `/settings/members` + `/settings/api-keys`), `/workspace` (renamed from `/settings/organization`, now reachable only via the sidebar's WorkspaceSwitcher, matching the mockup's lack of a dedicated nav item for it), and 6 simple `PlaceholderPage` routes (`/projects`, `/workflows`, `/usage`, `/audit`, `/integrations`, `/billing`) each citing its plan epic and phase. Old `/settings/*` directory deleted.
- [x] **Model picker** (`apps/web/src/components/chat/ModelPicker.tsx`, `lib/models.ts`, `store/chatStore.ts`'s new `selectedModelProfile`): backs plan E2's "model switching at conversation level" directly in the chat UI — clicking a model chip changes which model the *next* message uses; already-sent messages keep their own `modelProfile` regardless of later switches.
- [x] Re-themed every existing chat/members/dialog component to the new tokens; fixed a real bug found via visual testing: `Button` wasn't using `forwardRef`, so Radix's `asChild` (`DialogTrigger`, `AlertDialogCancel`) couldn't attach its ref, throwing a console warning — fixed by wrapping `Button` in `React.forwardRef`.

Build/lint/9 tests all still pass. Visually verified end-to-end with a Playwright driver script (no project run-skill existed yet) against the dev server: chat, team, conversations, billing placeholder, invite dialog, and the dark-mode toggle all render and function correctly.

### First real Docker Compose install — surfaced and fixed 3 latent bugs
Prior sessions had no Docker daemon available, so `make dev`/`make test`/`make lint` had never actually been run — only offline checks (`ast.parse`, `Base.metadata` registration, etc.) verified the backend. This session started Docker Desktop, ran `docker compose build`/`up` for real, and fixed what broke:

- [x] **`apps/api` package was never installing**: `pyproject.toml` declares `readme = "README.md"`, but that file didn't exist, so poetry's root-package install step silently failed every build (`ModuleNotFoundError: No module named 'afridock_api'` at container startup, masked at build time as a warning, not an error). Added `apps/api/README.md`.
- [x] **`pytest-bdd 7.0.*` incompatible with `pytest 8.2.*`**: `7.0.1` imports `iterparentnodeids` from `_pytest.nodes`, removed in pytest 8.2 — first-time dependency resolution (no lock file existed yet) pulled both pins together and they'd never actually been run in combination. Bumped `pytest-bdd` to `8.1.*` (supports pytest 8.2+); no test syntax changes needed.
- [x] **mypy had never been run for real**: 3 latent type errors surfaced — missing `types-PyYAML` stub (added to dev deps), `structlog`'s processor list needed an explicit `list[Processor]` annotation (`logging.py`), and `litellm.completion_cost` isn't in litellm's type stubs (added a narrow `# type: ignore[attr-defined]`). Also ran `ruff check --fix` + `black .` once for real, which reformatted a few pre-existing import-order/formatting issues in files never linted before.

Full stack now verified working end-to-end: `docker compose up -d` → all 4 services healthy, `GET /healthz` → `{"status":"ok"}`, web serves on 5173, `make test`/`make lint` both fully clean (backend 12 tests, frontend 9 tests). Local `node_modules` also synced for `apps/web` and `packages/shared` (editor/lint tooling outside Docker).

### Phase 1 — E1 Auth & Organizations, E2 Inference wiring, E3 Chat streaming, E4 History & Search
Full vertical slice: sign up → land in an isolated org → chat (streaming, with fallback) → conversation persists and is searchable. Implemented per a plan the user approved (`AskUserQuestion`/`ExitPlanMode` not needed here — this was researched and planned solo, then approved), sequenced E1 backend → E1 frontend → E2 → E3 → E4 → full-stack verification. Real Postgres/Redis throughout (this is the first Phase 1 work done against a live DB, not offline checks).

- [x] **E1 backend — auth & orgs** (`apps/api/src/afridock_api/{auth/,api/routes/{auth,organizations}.py,db/models/{user,organization}.py}`, migration `0002`): fastapi-users v13 with cookie+JWT transport (matches the frontend's existing `credentials: "include"` everywhere — zero fetch-layer changes needed); `User` extends fastapi-users' base with `org_id`/`role`/`display_name`; signup (`UserManager.create` override) always creates a brand-new `Organization` and makes the user its Admin; email verification required to log in (`requires_verification=True`) with the link **logged via structlog**, not emailed (no SMTP provider in the stack yet — `UserManager.on_after_register`/`on_after_forgot_password`); org member invite/list/update-role/remove endpoints reuse fastapi-users' reset-password flow for invite completion instead of a bespoke invite-token system.
- [x] **RLS bypass bug found and fixed**: migration `0001`'s RLS policies were silent no-ops — Postgres superusers *and* table owners always bypass RLS regardless of ENABLE/FORCE, and the API connected (and ran migrations) as `afridock`, the superuser `docker-compose` creates. Migration `0002` creates a non-superuser `afridock_app` role for the running API to connect as (`config.py`'s `database_url` vs `migrations_database_url` split); `FORCE ROW LEVEL SECURITY` added everywhere as defense-in-depth. **Two follow-on RLS bugs found while proving this end-to-end**, both fixed in the same migration: (1) the `users`/`organizations` tables need a "pass through when no org context is set yet" carve-out for login/signup itself (chicken-and-egg: can't look up a user by email, or insert a brand-new org, if RLS demands an org context that doesn't exist yet) — but pooled connections reset a touched custom GUC to `''`, not `NULL`, so the carve-out had to check `coalesce(current_setting(...), '') = ''`, not `IS NULL`; (2) that `OR`-based carve-out could still error, since Postgres doesn't guarantee `OR` short-circuits — the `::uuid` cast on the right side ran anyway on `''` — fixed with a `CASE WHEN ... THEN true ELSE ... END` (which *does* guarantee single-branch evaluation) instead. Proven correct via a raw-SQL test connecting *as* `afridock_app` directly (bypassing the app layer entirely) scoped to each org in turn.
- [x] **Alembic revision-id bug found and fixed**: the original descriptive revision strings (`"0001_conversations_messages_providers"`, 38 chars) overflowed Alembic's default `alembic_version.version_num VARCHAR(32)` column on the first-ever live migration run. Both migrations' `revision`/`down_revision` shortened to `"0001"`/`"0002"`; filenames stay descriptive.
- [x] **E2 — inference stub-fallback** (`inference/client.py`): `.env.example` already said "leave blank for local stub mode" — made that literal. `InferenceClient` checks whether any profile in the resolved chain has its provider's credential configured (`huggingface_api_token`/`openai_api_key`/`anthropic_api_key`, now `config.py` settings); if none do, it serves a canned, incrementally-streamed stub completion instead of raising `NoAvailableModelError` — the whole chat pipeline is demoable with zero secrets, and graduates to real inference the moment a key is set, no code changes. `stream()` extended to loop fallback candidates the same way `complete()` already did (falls back on a pre-first-token error only; mid-stream fallback stays out of scope per the existing docstring).
- [x] **E3 — chat streaming endpoint + persistence** (`api/routes/conversations.py`): `POST /conversations` (create), `GET /conversations/{id}/messages` (history), `POST /conversations/{id}/messages` (SSE, matches `useChatStream`'s existing `message`/`message_end`/`error` event contract exactly). **Real bug found via Playwright, not curl**: the assistant's reply was only persisted *after* the whole SSE generator completed — a client disconnecting mid-stream (navigating away) silently lost it. Fixed with a `finally` block... which itself needed a second fix: `SlowAPIMiddleware` is a Starlette `BaseHTTPMiddleware`, which runs the whole request in an anyio task group that cancels its scope on client disconnect — including new awaits inside that `finally` block — so the persistence call is now wrapped in `anyio.CancelScope(shield=True)` to survive it. Also: `send_message` deliberately does *not* use `Depends(get_org_session)` — FastAPI tears down `yield`-dependencies as soon as the handler *returns* the `StreamingResponse` object, before the body actually streams, so a dependency-injected session would already be closed by the time the generator runs; it uses a new `db/session.py:org_scoped_transaction` (a plain `@asynccontextmanager`) directly instead, opening short-lived transactions before and after streaming.
- [x] **E4 — conversation list + full-text search**: `GET /conversations?q=` (Postgres `plainto_tsquery`/`ts_rank` against the existing `search_tsv` trigger from migration `0001`). Personal history (scoped to `org_id` **and** `user_id`, not team-shared) — a deliberate reading of "conversations persist... their previous conversations are listed" as personal, ChatGPT-style history, satisfying "scoped to the user's org" a fortiori.
- [x] **E1 frontend — real auth**: `services/auth.ts` (login is form-urlencoded per fastapi-users' `OAuth2PasswordRequestForm`, not JSON like everything else), `store/authStore.ts` rewritten to hold the real session (populated via `hooks/useAuthBootstrap.ts` calling `GET /users/me`) instead of the hardcoded `"admin"` placeholder, `routes/{Login,Signup}Page.tsx`, `components/auth/AuthGuard.tsx`. Redirects use `window.location.href` (hard navigation), not `<Navigate>`/`useNavigate` — consistent with this router's existing jsdom/AbortSignal workaround (avoids client-side navigation firing on mount entirely).
- [x] **E3 frontend — real routing**: `/chat` creates a conversation (`useCreateConversation`) and hard-redirects to `/chat/:conversationId`; `ChatPanel` loads persisted history (`useConversationMessages`) into `chatStore` once per conversation (guarded by a ref, not just the query result, so a background refetch can't wipe out messages already streamed in since); `ConversationsPage` links to `/chat/:id` instead of a shared `/chat`.
- [x] **Real bugs found by the test suites (not just manual testing)**: `Sidebar.tsx`'s `user?.email.slice(...)` crashed on a null user — `?.` only makes the immediately-following access optional, not the rest of the chain; fixed to `user?.email?.slice(...)`. `LoginPage`/`SignupPage`'s `<label>`s weren't associated with their `<input>`s (no matching `htmlFor`/`id`) — an accessibility bug caught because Playwright's `getByLabel` (which mirrors the accessible-name computation screen readers use) couldn't find them either.

**Testing**: 25 new backend pytest tests (`tests/test_{auth_signup,tenant_isolation,chat_flow,organizations_rbac,inference_stub}.py`) against the live Postgres via `docker compose run --rm api pytest` — real RLS, real migrations, no mocked DB layer. A shared `event_loop` fixture (session-scoped) was needed in `conftest.py`: pytest-asyncio's default per-test event loop conflicts with `db/session.py`'s module-level asyncpg engine, created once at import time bound to whichever loop is running then. Full Playwright browser run (signup → verify-link → login → send message → stub response streams in → appears in conversations list → search matches/doesn't-match correctly → logout), zero console errors. `make test`/`make lint` clean across both apps throughout.

### Branding — product rename & logo assets (2026-07-23)
- [x] Renamed product from "Afridock" → "Afrikdock" across all frontend display strings (`Sidebar.tsx`, `AppShell.tsx`, `ChatInput.tsx`, `LoginPage.tsx`, `SignupPage.tsx`, `router.tsx`, `index.html`) — codebase/package name (`afridock_api`, repo name, `.env` vars, etc.) intentionally left as-is; this is a UI copy change, not a symbol rename.
- [x] Added real logo assets: `afrikdock_logo_primary.{png,svg}` (source), `apps/web/public/logo-icon.svg` (square mark, used as favicon + in-app), `apps/web/public/logo-lockup.svg` (full wordmark); removed the old placeholder `afridock_logo_light.png`.
- [x] Sidebar and auth pages now render the SVG logo icon instead of the placeholder "A" letterbox.

### Dev-tunnel access (cross-site CORS + cookie config) (2026-07-24)
- [x] Made cookie `SameSite`/`Secure` env-overridable (`API_COOKIE_SAMESITE`, `API_COOKIE_SECURE` in `config.py`/`auth/backend.py`) — default behavior (derive from `API_ENV`) unchanged for normal local/deployed use.
- [x] `.env` temporarily forwarded port 8000 through a VS Code dev tunnel for a specific debugging session — verified end-to-end (register → verify → login → `/users/me`) directly against the tunnel URLs, then **reverted back to `http://localhost:8000`/default cookie settings** once that tunnel session ended (its lingering config was actually found breaking the Phase 2 pytest suite and Playwright verification below — see Known issues).
- [x] Tunnel port visibility must be **Public**, not Private — Private tunnels redirect (302, no CORS headers) to a GitHub OAuth page, which breaks CORS preflight for any `fetch` caller.
- Note: `docker compose restart` does **not** reload `.env` (env vars are baked in at container creation); `docker compose up -d` is required to pick up `.env` changes.

### Phase 2 (foundational slice) — low-cost-open-source-AI constraint, E5 RBAC/API Keys, E6 Audit & Cost Attribution (2026-07-24)
User directive: continue implementing the plan with **"low-cost AI via open-source models" as the #1 design constraint driving every decision** — scoped (via `AskUserQuestion`) to the credential-free foundational slice this pass (constraint + E5 + E6), with E7–E10 (Slack/WhatsApp/Sheets/billing — all need real third-party developer credentials) explicitly deferred to a follow-up pass. Planned via `EnterPlanMode`/`ExitPlanMode`, approved before implementation.

- [x] **Constraint formalized in every governing document**: `Afridock_Implementation_Execution_Plan.md` §1.2 (new leading principle, outranks the other four), `CLAUDE.md` (Architecture principles, now 5 not 4), `AGENTS.md` (new non-negotiable rule #2), `README.md` (Ollama setup step) — not just prose: see the enforcement mechanism below.
- [x] **Self-hosted Ollama is now the real default inference path**, not just documented aspiration: new `ollama` service in `docker-compose.yml` (named volume, healthcheck), `make ollama-pull` (pulls `llama3.2:1b`, ~1.3GB one-time), new `llama-3.2-1b-instruct` profile (`profiles.yaml`, `provider: ollama`, `litellm_model: ollama_chat/llama3.2:1b`, `api_base: http://ollama:11434`, cost `$0`) now **first** in `default_chain`. Verified for real: sent a chat message through `InferenceClient` with zero cloud API keys configured and got a genuine Llama 3.2 1B response back (`model_profile: llama-3.2-1b-instruct`), not the canned stub.
- [x] **Two real bugs found and fixed while wiring Ollama in** (`inference/client.py`): (1) `mixtral-8x7b-instruct` (provider `vllm`) had *never* actually fired even before this pass — `_chain_has_credentials` only counted a candidate "available" if a cloud API key was configured, and self-hosted providers have none to configure, so it silently fell through to stub every time. Fixed with `SELF_HOSTED_PROVIDERS = {"ollama", "vllm"}` plus a new `_is_configured()` check (self-hosted needs a real `api_base`; cloud needs a key) used both for the stub-vs-real gate and skipped per-candidate inside the fallback loop. (2) Without the per-candidate skip, adding Ollama to the default chain meant an *unconfigured* cloud candidate (e.g. `llama-3.1-8b-instruct` with no HF token) would still be attempted for real and could surface a raw, unnormalized provider exception (`HuggingfaceError` wrapping a DNS failure) instead of being skipped cleanly — caught by running the full pytest suite, not assumed. Also fixed: `litellm.completion_cost()` returns `None` (not `0.0`) for models outside its pricing map (e.g. Ollama) — `InferenceResult.cost_usd` is a `float`, so this would have raised a validation error the first time `complete()` (not just `stream()`) was used against a self-hosted model.
- [x] **Org-level commercial-fallback gate — the constraint's actual enforcement mechanism**: new `Organization.allow_commercial_fallback` column (migration `0003`, default `false`); `FallbackChain` gained `allow_commercial: bool`, filtering out `is_commercial: true` profiles (`claude-fallback`/`gpt-fallback`, now flagged in `profiles.yaml`) unless the org has opted in. `send_message` loads the flag inside its existing `org_scoped_transaction` block. Net effect: no organization can incur a real per-token commercial cost without an explicit Admin opt-in. `PATCH /organizations/current/settings` (Admin-only) + `GET /organizations/current` expose it; `WorkspacePage.tsx` (previously a placeholder) now shows real org name + the toggle, with cost-warning copy.
- [x] **E5 — API keys**: new `ApiKey` model/table (migration `0003`, org-scoped RLS with the same "pass through when unscoped" carve-out as `users`/`organizations` — an API key must be looked up by hash *before* its org is known), SHA-256-hashed storage (not bcrypt/argon2 — hashing a high-entropy random token, not a user password, so a slow KDF buys nothing), plaintext shown exactly once at creation (`api/routes/api_keys.py`, Admin-only CRUD). API-key auth (`Authorization: Bearer afk_...`) wired into `GET /organizations/current/members` specifically via a new hybrid dependency (`get_org_id_via_cookie_or_api_key`, tries the header first, falls back to the cookie session) — deliberately not retrofitted into the SSE chat pipeline, which was hardened through several subtle async bugs last pass and isn't worth that risk for what the Gherkin actually requires. `TeamPage.tsx`'s `ApiKeysSection` (previously a placeholder) now has real create/list/revoke UI (`CreateApiKeyDialog`, `ApiKeysTable`, `RevokeApiKeyAlertDialog`).
- [x] **E5 — RBAC scenario outline, and a real gap it found**: added the plan's exact Admin/User/Viewer × action Gherkin table as named pytest tests (`test_rbac_scenarios.py`). Writing the "Viewer cannot send a chat message" case found that `create_conversation`/`send_message` (`conversations.py`) had **no role check at all** — any active Viewer could already send chat messages, contradicting the plan. Fixed by gating both with `require_role(ADMIN, USER)`.
- [x] **E6 — cost/audit logging**: `InferenceUsageLog` rows are now written from `send_message`'s `persist_assistant_message` (previously the table existed but nothing wrote to it). Token counts are **estimated** via `litellm.token_counter` (the Gherkin literally says "an estimated cost is computed" — exact provider-returned usage needs per-provider `stream_options` support not every target here honors); cost is computed from the existing per-profile rates in `profiles.yaml`, which are `$0` for every open-source/self-hosted profile — making the audit trail itself a visible proof point of the low-cost constraint. New `GET /organizations/current/usage` (any active member — aggregate + per-model breakdown) and `GET /organizations/current/audit` + `.../audit/export.csv` (Admin-only, matches "Viewers cannot access the audit export"). Real `UsagePage.tsx` (recharts bar chart, single series/one hue, stat tiles) and `AuditPage.tsx` (table + CSV export link) replace their `PlaceholderPage`s.
- [x] **Real flakiness bug found and fixed**: once this pass's ~14 new tests were added, the full backend suite started intermittently failing — traced to `slowapi`'s global `60/minute`-per-IP default (all tests share one "IP" via the in-process ASGI transport) being exceeded by the suite's own auth traffic, non-deterministically depending on wall-clock timing against the 60s window. Fixed with an autouse session-scoped `conftest.py` fixture that disables the limiter for the test session — confirmed via 4 consecutive clean full-suite runs (was previously failing ~2 out of every 3).

**Testing**: 14 new backend pytest tests (`test_api_keys.py`, `test_rbac_scenarios.py`, `test_usage_and_audit.py`, plus updates to `test_inference_fallback.py`/`test_inference_profiles.py`/`test_chat_flow.py` for the new default chain and commercial-fallback gate) — 42 total, all passing, real Postgres/Ollama throughout. Frontend: build/lint/9 Vitest tests clean. Full Playwright pass (signup → verify → login → **real Ollama chat response, confirmed non-stub** → toggle commercial fallback → create/revoke an API key → usage dashboard renders → audit log + CSV export link present), zero console errors. Found and fixed a real accessibility bug the same way as Phase 1's Login/Signup pages: `CreateApiKeyDialog`'s "Name" label wasn't associated with its input (`htmlFor`/`id`) — caught because Playwright's `getByLabel` couldn't find it either.

### Self-hosted NVIDIA NIM inference profile (2026-07-24)
User directive: add `NVIDIA_NIM_API_KEY`/`NVIDIA_NIM_BASE_URL` (a DGX Spark box) as another inference model. Clarified via `AskUserQuestion` (model tag, cost treatment, chain position) before implementing.

- [x] New `llama-3.1-70b-nim` profile (`profiles.yaml`, `provider: nvidia_nim`, `litellm_model: openai/meta/llama-3.1-70b-instruct` — NIM exposes an OpenAI-compatible API, same pattern as the existing `vllm` profile), placed **first** in `default_chain` (a full-size 70B on dedicated hardware, tried before the in-container 1B Ollama model). Treated as self-hosted/free, not commercial — it's the user's own hardware, zero marginal per-token cost, no `allow_commercial_fallback` gating. **Renamed to `llama-3.1-8b-nim` once wired to the real box — see the "DGX Spark goes live" entry below: `GET /v1/models` confirmed it actually serves the 8B model, not 70B.**
- [x] `SELF_HOSTED_PROVIDERS` (`inference/client.py`) extended to include `nvidia_nim`. Unlike Ollama's fixed Docker Compose hostname, a DGX Spark's address is per-deployment, so `profiles.yaml` deliberately leaves this profile's `api_base` unset; a new `_api_base_for()` resolves it dynamically from `Settings.nvidia_nim_base_url` at call time (mirrors how `_api_key_for()` already resolves cloud-provider keys). `_is_configured()`/both `litellm.acompletion` call sites updated to go through this resolver.
- [x] Left `NVIDIA_NIM_API_KEY`/`NVIDIA_NIM_BASE_URL` **commented out** in the real (gitignored) `.env`, blank in `.env.example`: a non-empty placeholder URL there would count as "configured" and every chat request would try (and fail/timeout against) an unreachable host before falling back to Ollama — uncomment with the real DGX Spark address once that NIM container is actually reachable.
- [x] 3 new unit tests (`test_inference_client.py`): NIM profile is unconfigured when the base URL is blank, becomes configured once set, and confirms Ollama's static `api_base` is unaffected by the new dynamic-resolution path. 45 backend tests total, ruff/black/mypy clean.

### Local-dev signup friction fix: auto-verify via a dev-only token endpoint (2026-07-24)
User hit the "check the API server's logs for your verification link" message while signing up in the browser and asked to fix it — real friction with no informational value, since a real end user already knows local dev has no email provider.

- [x] New `POST /auth/dev/verification-token` (`api/routes/auth.py`, `include_in_schema=False`): given an email, mints the exact same verification JWT `UserManager.request_verify` would (same secret/audience/lifetime, read off `UserManager` rather than hardcoded, so it can't drift), and returns it directly instead of only logging it. **404s outside `API_ENV=local`** so this endpoint never exists in a real deployment; also 404 for an unknown email and 400 for an already-verified user. Reuses the existing `/auth/verify` route rather than a bespoke auto-verify path, so the exact same code path a real email click-through would hit still runs.
- [x] `SignupPage.tsx` now calls this endpoint right after registering; if it returns a token (local dev), the page verifies it, logs the user in, and redirects straight to `/chat/:id` — zero manual steps. If it 404s (any real deployment), falls back to the pre-existing "check your verification link" screen, with its copy changed from "ask whoever runs the API server to check its logs" to "check your email for a verification link" (the log-digging instruction was local-dev-specific and wrong to show in a real deployment context).
- [x] Verified for real: 4 new backend pytest tests (token round-trips through `/auth/verify`, 404 unknown email, 400 already-verified, 404 when `API_ENV` isn't `local`) — 49 backend tests total. Playwright confirmed the full browser flow (signup → lands directly on a live `/chat/:id` session, no manual verification step), zero console errors.

### NVIDIA's hosted API catalog as a second inference profile (2026-07-24)
User directive: add `https://integrate.api.nvidia.com/v1` (`meta/llama-3.3-70b-instruct`) as another model. Distinct from the DGX Spark profile above — this is NVIDIA's own paid cloud infrastructure, not self-hosted hardware. Clarified via `AskUserQuestion` (cost treatment, chain position) before implementing.

- [x] New `llama-3.3-70b-nvidia` profile (`profiles.yaml`, `provider: nvidia_cloud`, `litellm_model: openai/meta/llama-3.3-70b-instruct`, static `api_base` — NVIDIA's public endpoint is fixed, unlike the DGX Spark box, so it doesn't need the dynamic-settings resolution the NIM profile does). Per the user's choice, categorized like the existing Hugging-Face-hosted entries (not `is_commercial`, $0 in the registry) rather than gated behind `allow_commercial_fallback` — an open-weight model via a hosted API, consistent with this repo's existing precedent for HF, even though NVIDIA's hosting itself isn't guaranteed free at any volume. Placed third in `default_chain`, right after the DGX Spark NIM profile and in-container Ollama, before the HF entries. New `Settings.nvidia_api_key` / `NVIDIA_API_KEY` env var, distinct from the DGX Spark's `NVIDIA_NIM_API_KEY`.
- [x] **Real bug found and fixed via a live call to the real NVIDIA endpoint** (`inference/client.py`): `litellm.completion_cost()` doesn't just return `None` for a custom `openai/<name>` model string outside its pricing map (already handled from the DGX Spark pass) — it can also *raise* `NotFoundError`. Confirmed against a genuine NVIDIA-cloud response for `openai/meta/llama-3.3-70b-instruct`. Now caught and defaulted to `$0.0`, same rationale as the `None` case — no pricing entry means $0 by definition for self-hosted/hosted-open-weight profiles in this registry, not a crash. This also protects the still-unconfigured `mixtral-8x7b-instruct`/`llama-3.1-70b-nim` profiles the first time either goes genuinely live.
- [x] `test_chat_flow.py`'s stub-mode fixture only blanked `SELF_HOSTED_PROVIDERS`, relying on cloud-provider keys already being blank in a fresh `.env` — broke the moment a real `NVIDIA_API_KEY` was configured (the test made a real, ~120s-timing-out network call instead of getting the canned stub). Fixed by having the fixture explicitly blank every cloud-provider env var (matching `test_inference_stub.py`'s existing pattern), so it no longer depends on what happens to be in a developer's real `.env`.
- [x] **Two live secrets, pasted directly into the git-tracked `.env.example` (not the gitignored `.env`), caught and removed before any commit**: a second NVIDIA API key and a Hugging Face token both ended up in `.env.example` mid-session (the file the user had open in their IDE) rather than `.env` — confirmed via `git log --all -- .env.example` that neither was ever committed, so no history rewrite was needed; both were blanked back to placeholders. Flagged directly to the user, including that the *first* NVIDIA key (pasted in chat, not a file) is now in this conversation's transcript and worth rotating if that transcript is ever shared or retained.
- [x] 4 new tests (`test_inference_client.py`): NVIDIA-cloud profile unconfigured/configured based on `NVIDIA_API_KEY`, and a regression test simulating `litellm.completion_cost` raising `NotFoundError` (mocked, not a real network call) confirming `cost_usd` still defaults to `0.0`. 52 backend tests total, ruff/black/mypy clean. Verified for real against the live NVIDIA endpoint: `client.complete()` returned a genuine "Acknowledged" completion with `cost_usd: 0.0`.

### DGX Spark NIM goes live, and two more real bugs found (2026-07-24)
User provided the DGX Spark's real LAN address (`http://192.168.18.199:8001/v1`, confirmed reachable — wired ethernet chosen over Wi-Fi after testing both) to finally activate the `nvidia_nim` profile stubbed out earlier.

- [x] **Model-name correction**: `GET /v1/models` against the live NIM server revealed it actually serves `meta/llama-3.1-8b-instruct`, not the `70b` this profile was originally configured for (a plan-time assumption, never empirically checked until the box was reachable). Renamed the profile `llama-3.1-70b-nim` → `llama-3.1-8b-nim` and fixed its `litellm_model` string to match — every prior reference (tests, this file) updated accordingly. Verified for real: a direct `client.complete()` call returned a genuine response in 597ms (LAN latency, much faster than either cloud path).
- [x] **`NVIDIA_NIM_API_KEY` set, `NVIDIA_NIM_BASE_URL` still deliberately commented out mid-session**: a second live key (originally miscategorized as the NVIDIA-cloud key when it leaked into `.env.example`, corrected once the user clarified it was actually for this DGX Spark) was added to `.env` before the real base URL was known — confirmed via `_is_configured()` that the key alone doesn't activate the profile, so no premature calls to a placeholder host occurred in the gap.
- [x] **Real bug: `InferenceClient.stream()`'s fallback logic didn't actually cover its own documented contract.** Its docstring already promised "falling back... on any error raised before the first token," but the implementation only wrapped the initial `acompletion()` call in try/except — litellm's Hugging Face backend (confirmed empirically, not assumed) defers its actual TCP connect to the *first chunk fetched from the async generator*, not to `acompletion()` itself. A DNS failure there (this sandbox can reach NVIDIA's cloud endpoint and the DGX Spark's LAN IP directly, but not Hugging Face's real API hostname) raised a raw, litellm-unnormalized `httpx.ConnectError` that was never caught — crashing the whole SSE stream (`ERR_INCOMPLETE_CHUNKED_ENCODING` in the browser) and, worse, silently persisting an **empty assistant message with `status: normal`** misattributed to a model that never actually answered, instead of surfacing an error or falling back. Found via a real browser session, confirmed by querying the persisted row directly (a Playwright "page has some text on it" check had falsely read as success — loose assertion, not proof). Fixed: the per-candidate chunk-iteration loop now tracks whether any content has actually reached the caller; any exception before that point falls back to the next candidate (cooldown + `continue`, same as a connection error at the initial call), and only re-raises once content has genuinely started streaming — preserving the existing "no silent model switch mid-message" guarantee. `result.model_profile`/`litellm_model` are now only set once a real chunk arrives, fixing the misattribution too. New regression test (`test_stream_falls_back_when_a_provider_fails_before_first_token`) simulates exactly this lazy-connect-failure shape without a real network call.
- [x] **Real bug: the frontend's model picker was stale and out of sync with the actual registry.** `lib/models.ts`'s `MODEL_OPTIONS` (hardcoded, "keep in sync with profiles.yaml manually") had never been updated across three profile-adding passes this session — Ollama, the DGX Spark, and NVIDIA's hosted catalog were all invisible/unselectable in the UI. `chatStore.ts`'s default `selectedModelProfile` was still hardcoded to `llama-3.1-8b-instruct` (Hugging Face) — directly contradicting CLAUDE.md's #1 constraint by defaulting every new chat to a paid-API-shaped, DNS-unreachable-in-this-sandbox model instead of the self-hosted option that's actually always available. Fixed: `MODEL_OPTIONS` now lists all 8 profiles; default changed to `llama-3.2-1b-instruct` (Ollama) specifically — not the DGX Spark or NVIDIA cloud, since those are this-deployment-specific, while Ollama is the one self-hosted option every install of this stack genuinely has out of the box.
- [x] Verified end-to-end for real, twice: a direct `curl` SSE reproduction and a full Playwright browser run (signup → auto-verify → real Ollama chat response → persisted correctly with `provider: ollama`, `model_id: llama-3.2-1b-instruct` — confirmed via direct DB query, not just page text) both came back clean, zero console errors, usage dashboard showing 1 request / real token count / $0 cost.

**Testing**: 1 new regression test (53 backend total), ruff/black/mypy clean; frontend build/lint/9 Vitest tests clean.

## In progress

_(nothing — claim work here before starting it)_

| Epic / story | Owner (agent/session) | Started | Notes |
|---|---|---|---|

## Next up (Phase 2 continued — Integrations & Billing, see plan §Phase 2)

- [ ] **E7 — Slack Integration (M)**
- [ ] **E8 — WhatsApp Business Integration (M)**
- [ ] **E9 — Google Sheets Integration (S)**
- [ ] **E10 — Usage-Based Billing, Paystack/Flutterwave (M)**

Explicitly deferred from this pass (not silently dropped): E7–E10 all need real third-party developer credentials (Slack app, Meta WhatsApp Business, Google OAuth, Paystack/Flutterwave) to be more than sandbox scaffolding — can be built in stub/sandbox mode per the plan's own testing strategy (§5.1), but that's a distinct, clearly-scoped follow-up pass. Also still deferred from Phase 1: Authlib/SSO, Celery/offline queue, real outbound email. Making stub-mode strictly local-only (currently "no configured candidate" → stub regardless of `API_ENV`) is good hardening but wasn't in this pass's scope — see Known issues.

Later phases: Phase 3 E11–E15 (offline queue, caching, PWA, security, launch), Phase 4 E16–E19, Phase 4 E20 Projects (proposed — see plan, not yet confirmed).

## Decisions & deviations log

Record any decision that deviates from or refines the execution plan.

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-21 | Created progress.md as mandatory pre-coding checkpoint for all agents | Keep multi-agent/multi-session work coordinated against the phased plan |
| 2026-07-21 | Backed the progress.md policy with a Claude Code hook, a git pre-commit hook, and a CI check (see "Enforcement" above) | A prose instruction in CLAUDE.md is not deterministic — an agent can forget or skip it; mechanical checks at the tool, commit, and PR layers can't be silently ignored |
| 2026-07-21 | Researched langgenius/dify (shallow clone, 4 parallel research agents, spot-checked against the actual repo) and adapted 4 patterns rather than designing from scratch: chat streaming/markdown, admin RBAC dashboard, LLM-provider abstraction, conversation/message schema | Avoid reinventing solved problems; Dify is a mature, similar-domain (multi-provider LLM platform) open-source reference |
| 2026-07-21 | Departed from Dify's schema: one row per turn (not query/answer pairs) with per-message model attribution, `org_id`-scoped Postgres RLS (Dify isolates only at the app layer), real `tsvector`/pgvector columns (Dify has neither on chat tables) | Plan requires hard multi-tenant isolation at the data layer and mid-conversation model switching with attribution preserved — Dify's shape doesn't give us either directly |
| 2026-07-21 | Kept only a thin abstraction over LiteLLM (`ModelProfileRegistry` + `FallbackChain` + `InferenceClient`) rather than Dify's full plugin-daemon/manifest provider system | LiteLLM already normalizes provider SDKs the way Dify's extracted `graphon` package does; the plugin-daemon exists to let third parties ship out-of-process providers, which is out of scope for the plan |
| 2026-07-21 | Used real React Router routes (originally under `/settings/*`, later flattened to top-level — see below) instead of Dify's single account-setting modal; flat `Role → Capability[]` map instead of Dify's granular permission-key RBAC | Deep-linkable routes are more idiomatic outside Next.js; the plan only needs 3 fixed roles (Admin/User/Viewer), so Dify's indirection for arbitrary custom roles is unneeded complexity |
| 2026-07-21 | Adopted the user-supplied design mockup's Classical editorial visual theme (not the original dark slate/emerald palette) as the default, via a swappable CSS-variable token layer with both a light (default) and dark override | User's explicit choice ("support both via a token layer") after being asked; the mockup was a fully-worked design, not a placeholder, and the plan itself doesn't mandate a specific visual language |
| 2026-07-21 | Built the full 10-item nav shell from the mockup now, with simple phase-gated `PlaceholderPage` placeholders (text notice + epic/phase citation, no fake data) for anything not yet in scope, rather than only showing in-phase nav items | User's explicit choice; matches the pattern already used for Billing/API Keys before this pass — avoids restructuring the sidebar repeatedly as each phase starts |
| 2026-07-21 | Flattened `/settings/*` (nested SettingsLayout with its own sub-nav) into top-level routes (`/team`, `/workspace`, `/billing`, etc.) under one AppShell sidebar; "API Keys" lives as an in-page tab on `/team` rather than its own nav item, since the mockup has none | The mockup has one sidebar, not a nested settings area with its own — Team & Roles, Integrations, and Billing are separate top-level items in it, not sub-tabs |
| 2026-07-21 | Deferred "Projects" (a mockup nav item/concept not in any epic) to a nav-only placeholder now; logged as candidate epic **E20** in the plan's Phase 4 (Could-have, unconfirmed), with its real data model (a `projects` table + project-level membership layer) explicitly deferred until confirmed | User's explicit choice ("make it a placeholder but document it... so it will not be forgotten"); a project-level membership layer is a real scaling/complexity cost (two-level RBAC) not worth taking on unscoped |
| 2026-07-23 | Bumped `pytest-bdd` from `7.0.*` to `8.1.*`; added `types-PyYAML` dev dependency; added `apps/api/README.md` | First real `docker compose build`/`up` (Docker wasn't available in prior sessions) surfaced that these were never actually compatible/complete — see "First real Docker Compose install" above |
| 2026-07-23 | Auth transport is cookie + JWT (fastapi-users), not bearer-header | `apps/web`'s fetch wrapper and SSE stream already send `credentials: "include"` everywhere — needs zero frontend fetch-layer changes |
| 2026-07-23 | Single org per user (no membership join table); signup always creates a brand-new org; invited users join the inviter's org via fastapi-users' reset-password flow reused as an invite-completion mechanism | Matches E1's Gherkin (one user, one isolated org) and Team page's existing invite UI without building a bespoke invite-token system |
| 2026-07-23 | Created non-superuser `afridock_app` Postgres role for the running API; migrations still run as the superuser `afridock` | Postgres superusers unconditionally bypass RLS regardless of ENABLE/FORCE — the app must never query through the migrations connection, or the plan's "hard isolation via RLS" principle is a no-op (see "RLS bypass bug" above) |
| 2026-07-23 | Conversation history is personal (scoped to `org_id` AND `user_id`), not team-shared | Matches typical chat-product UX (ChatGPT-style); still trivially satisfies E4's "scoped to the user's organization" requirement as a subset. Revisit if team-shared history is ever wanted — would need a product decision, not just a query change |
| 2026-07-23 | Assistant-reply persistence in `send_message` runs in a `finally` block wrapped in `anyio.CancelScope(shield=True)`, and uses its own `org_scoped_transaction` rather than `Depends(get_org_session)` | Two independent, real bugs found via Playwright (not curl): (1) FastAPI tears down yield-dependencies as soon as the handler returns the `StreamingResponse`, before the body streams — a dependency session would already be closed when the generator runs; (2) `SlowAPIMiddleware`'s `BaseHTTPMiddleware` cancels the whole request's anyio scope on client disconnect, which would cancel new awaits in a bare `finally` block too, without the explicit shield |
| 2026-07-23 | Product renamed "Afridock" → "Afrikdock" in all UI copy only (not code symbols/package/repo names) | User's explicit choice — the supplied logo's wordmark reads "Afrikdock"; user asked to make the app name match rather than treat the logo text as wrong |
| 2026-07-24 | Cookie `SameSite`/`Secure` made env-overridable rather than hardcoded to the `api_env != "local"` derivation | Needed to support a VS Code dev-tunnel frontend, which is cross-site from the API — `SameSite=Lax` silently drops the cookie on cross-site fetch/XHR, and `SameSite=None` requires `Secure`, which in turn requires the response that sets it to be HTTPS (so the API's own tunnel had to also be Public, not Private — Private tunnels 302-redirect to a GitHub OAuth page with no CORS headers, breaking preflight) |
| 2026-07-24 | "Low-cost AI via open-source models" elevated to the #1 architectural principle in the plan/CLAUDE.md/AGENTS.md, outranking the other four when they conflict | User's explicit directive for this pass; the plan already leaned this way (open-source-first fallback ordering) but never stated it as a ranked, enforced constraint — this pass makes it one, with a concrete enforcement mechanism (see next row), not just prose |
| 2026-07-24 | Commercial model profiles (`is_commercial: true`) are only ever fallback candidates when `Organization.allow_commercial_fallback` is explicitly `true` (default `false`) | The mechanism that makes the above constraint real rather than aspirational — without it, a Llama/Mixtral failure would silently escalate to a real per-token Claude/OpenAI bill with no org consent |
| 2026-07-24 | Self-hosted Ollama (`llama-3.2-1b-instruct`) added as the first entry in `default_chain`, ahead of Hugging Face's Inference API | HF's Inference API is often paid/gated for real models at any scale; a genuinely free, self-hosted, zero-credential model is the more honest "low-cost open-source" default for local/self-hosted deployments — chosen at 1B params specifically so it runs on CPU in a resource-constrained sandbox/laptop, not because it's the production-recommended size |
| 2026-07-24 | API-key auth (E5) wired into `GET /organizations/current/members` only, not the chat/completions endpoints | The Gherkin only requires proving a key is authorized-to-its-org and revocable; retrofitting API-key identity (which has no `user_id`) through `send_message`'s already-hardened async/cancellation logic wasn't worth the risk for that requirement — extending it is natural future work once there's a concrete external-API use case |
| 2026-07-24 | E6 cost/token figures are **estimated** (`litellm.token_counter` on the accumulated request/response text), not provider-returned exact usage | The Gherkin explicitly says "an estimated cost is computed"; exact usage in the streaming path needs per-provider `stream_options` support that self-hosted Ollama/vLLM and not every hosted provider here honors uniformly — estimation is simpler and sufficient |
| 2026-07-24 | slowapi's rate limiter is disabled for the whole pytest session (`conftest.py` autouse fixture), not scoped down in production | This pass's new tests pushed the suite's own auth traffic over the global `60/minute` limit, causing non-deterministic failures depending on wall-clock timing — tests exercising real auth flows repeatedly should never be throttled by a production safety limit; scoping the *production* limit to `/auth/*` specifically remains a separate, still-deferred item |
| 2026-07-24 | Self-hosted NVIDIA NIM (`llama-3.1-70b-nim`) treated as free/self-hosted (like Ollama/vLLM), not `is_commercial`, and placed first in `default_chain` | User's own DGX Spark hardware — zero marginal per-token cost, so it doesn't need `allow_commercial_fallback` consent; a full 70B on dedicated hardware is a better default than the in-container 1B Ollama model when reachable |
| 2026-07-24 | NIM's `api_base` is resolved dynamically from `Settings` (`_api_base_for()`) rather than hardcoded in `profiles.yaml` like Ollama's | Ollama's endpoint is a fixed Docker Compose service hostname; a DGX Spark's address varies per deployment/machine, so it has to come from env config, not a checked-in file |
| 2026-07-24 | Added a `POST /auth/dev/verification-token` endpoint (undocumented, `API_ENV=local`-gated) that mints and returns a real verification JWT, rather than auto-verifying users server-side or exposing the structlog line some other way | Auto-verifying would silently bypass the tested "login rejected until verified" contract (`test_login_is_rejected_until_verified_then_succeeds`) for every signup, even ones a future test wants to exercise unverified; minting-and-returning the same token instead keeps that contract intact while removing the friction only for the interactive local-dev signup path |
| 2026-07-24 | NVIDIA's hosted API catalog (`llama-3.3-70b-nvidia`) categorized like the Hugging-Face-hosted entries (not `is_commercial`), not gated behind `allow_commercial_fallback` | User's explicit choice — an open-weight model accessed via a hosted API, same bucket as the existing HF profiles, even though (unlike the DGX Spark box) NVIDIA's own hosting isn't guaranteed free at any volume |
| 2026-07-24 | `litellm.completion_cost()` failures caught for both its `None`-return and `NotFoundError`-raise cases, not just the former | Confirmed via a real call to NVIDIA's hosted API that custom `openai/<name>` model strings can make litellm raise instead of returning `None` for pricing lookups it can't resolve — the DGX Spark/vLLM profiles hit this same class of model string, just not yet exercised live |
| 2026-07-24 | `InferenceClient.stream()`'s per-candidate fallback now wraps the chunk-iteration loop, not just the initial `acompletion()` call, falling back on any error before the first real token reaches the caller | litellm's Hugging Face backend defers its actual connection attempt to the first chunk fetch, not the initial call — a failure there raised an unnormalized exception that used to crash the SSE stream and persist an empty, misattributed "successful" message instead of falling back, contradicting this method's own pre-existing docstring |
| 2026-07-24 | Frontend's default `selectedModelProfile` changed from the hardcoded `llama-3.1-8b-instruct` (Hugging Face) to `llama-3.2-1b-instruct` (Ollama); `lib/models.ts`'s `MODEL_OPTIONS` list resynced to include all 8 current registry profiles | The old default directly contradicted CLAUDE.md's #1 constraint by defaulting every new chat to a cloud-hosted-API-shaped model instead of the self-hosted option every deployment of this stack actually has; Ollama (not the DGX Spark or NVIDIA cloud, both this-deployment-specific) is the right *portable* default |
| 2026-07-24 | Centralized provider-credential blanking into a single session-wide `conftest.py` autouse fixture (`_no_real_inference_credentials`), removing the duplicated per-file versions in `test_chat_flow.py`/`test_inference_stub.py` | A real `HUGGINGFACE_API_TOKEN` added to `.env` broke `test_usage_and_audit.py` (no local override existed there) with a real, ~timing-out network call — the whole suite needs this guarantee, not just the two files that happened to need it before |

## Known issues / blockers

- No project run-skill exists yet for launching `apps/web`/`apps/api` — every session so far has driven the dev server with an ad hoc Playwright script (see `/run` skill's guidance to capture this as a project skill via `/run-skill-generator` next time it's needed).
- The frontend build emits a "chunks larger than 500 kB" warning (mainly Shiki's per-language grammar chunks via `@streamdown/code`, lazy-loaded on demand) — non-blocking, not new debt from this session beyond a modest size increase, but worth code-splitting attention eventually.
- `pytest-asyncio` emits a deprecation warning for `conftest.py`'s session-scoped `event_loop` fixture override (`tests/conftest.py`) — functionally correct on `pytest-asyncio 0.23.8`, but that fixture-override mechanism is slated for removal in a future `pytest-asyncio` release. Revisit with `asyncio_default_fixture_loop_scope`/per-test `loop_scope` markers if/when `pytest-asyncio` is upgraded.
- `list_conversations` does one extra query per conversation for message-count/preview (N+1) — fine at MVP scale (dozens per user), flagged in a code comment for whoever revisits it once that stops being true.
- No automated regression test exercises the mid-stream-disconnect persistence fix directly (see decisions log) — faithfully simulating a real ASGI client disconnect through the in-process `httpx.ASGITransport` test client proved unreliable (it doesn't clearly signal `http.disconnect` the way a real socket close does), so this was verified via the real Playwright browser run instead. A true regression test would need either a real running server + real HTTP client, or a more invasive ASGI-level fake.
- E1's Gherkin scenario "the fallback event is recorded in logs and metrics" — the *logs* half is done (`inference.fallback_used` structlog event); metrics (Prometheus) aren't wired for this yet.
- Stub-mode (`inference/client.py`) isn't gated by `API_ENV` — "no configured candidate available" serves the canned stub response regardless of environment, including a deployed production one. Fine for local dev; a real production deployment with a genuinely broken model chain would silently show fake content instead of a real error. Good hardening for a future pass, not done in this one.
- `mixtral-8x7b-instruct` (self-hosted vLLM profile) still has no `api_base` configured — it's correctly skipped without a network call now (see the `_is_configured` fix above) but needs a real vLLM endpoint (via a `model_profile_override`) before it can actually serve traffic.
- `llama-3.1-8b-nim` (self-hosted NVIDIA NIM/DGX Spark profile, renamed from `llama-3.1-70b-nim`) is now genuinely live — `NVIDIA_NIM_API_KEY`/`NVIDIA_NIM_BASE_URL` both set in `.env` to the real DGX Spark LAN address, verified end-to-end via both a direct `client.complete()` call and a full browser session.
- First boot only: if a chat request arrives while `make ollama-pull`'s model download is still in progress, Ollama isn't yet a genuinely usable candidate and the request may surface a real connection error instead of gracefully waiting or falling back to stub — narrow, time-bounded (a few minutes on first clone), not fixed this pass.
- Rate limiting is disabled entirely for the pytest session (see decisions log) — the production limiter itself is unchanged (still API-wide `60/minute`, not scoped to `/auth/*`); that remains deferred.
- Deferred (see Next up): E7–E10 (Slack/WhatsApp/Sheets/billing), Authlib/SSO, Celery/offline queue, real outbound email.

## How to update this file

1. Before coding: read this file top to bottom; confirm your task matches the current phase and isn't already in progress.
2. Claim your task in **In progress** (epic/story, short note).
3. After finishing: move it to **Completed** (with checkbox), update **Next up**, log any decisions/deviations, and refresh the **Last updated** date.
4. Keep entries terse — one line per item; link to plan sections or files rather than duplicating detail.
